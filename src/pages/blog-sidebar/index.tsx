import { Layout } from '@/components/layout';
import { ReactNode } from 'react';

const BlogSidebarPage = () => <></>;

export default BlogSidebarPage;

BlogSidebarPage.getLayout = (page: ReactNode) => <Layout>{page}</Layout>;
