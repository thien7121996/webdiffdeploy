// import { DiffImages } from '@/components/pages/CompareImageDetail/DiffImages';

export const Snapshot = () => {
  // const {
  // 	diffRef,
  // 	inputARef,
  // 	diffCount,
  // 	inputBRef,
  // 	imageWrapperRef,
  // 	imageACanvasRef,
  // 	imageBCanvasRef,
  // 	handleUploadImage,
  // } = useCompareImage();

  return (
    <>
      {/* Image A:
			<input
				// onChange={event => handleUploadImage(event, INPUT_TYPE.INPUT_A)}
				ref={inputARef}
				type="file"
			/>
			<br />
			Image B:
			<input
				// onChange={event => handleUploadImage(event, INPUT_TYPE.INPUT_B)}
				ref={inputBRef}
				type="file"
			/>
			<br />
			{diffCount}
			<br />
			<DiffImages
				imageACanvasRef={imageACanvasRef}
				imageBCanvasRef={imageBCanvasRef}
				imageWrapperRef={imageWrapperRef}
				diffRef={diffRef}
			/> */}
    </>
  );
};
